# Email Agent Configuration
# Copy this file to .env and fill in your credentials

# =============================================================================
# ANTHROPIC API KEY (Required)
# =============================================================================
# Your Anthropic API key for Claude access
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# =============================================================================
# IMAP SERVER CONFIGURATION (Required)
# =============================================================================
# IMAP server hostname
# Gmail: imap.gmail.com
# Outlook: outlook.office365.com
# Yahoo: imap.mail.yahoo.com
IMAP_HOST=imap.gmail.com

# IMAP server port (993 for SSL/TLS)
IMAP_PORT=993

# =============================================================================
# EMAIL CREDENTIALS (Required)
# =============================================================================
# Your email address
EMAIL_ADDRESS=your-email@gmail.com

# For Gmail: Use an App Password, NOT your regular password!
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate a new App Password for "Mail"
# 4. Use the 16-character password here (spaces optional)
EMAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# =============================================================================
# APPLICATION SETTINGS (Optional)
# =============================================================================
# Maximum number of emails to fetch per sync (default: 50)
MAX_EMAILS_TO_FETCH=50

# Email sync interval in seconds (default: 300 = 5 minutes)
SYNC_INTERVAL_SECONDS=300

# SQLite database path (default: priv/emails.db)
DATABASE_PATH=priv/emails.db

# Log level: debug, info, warning, error (default: info)
LOG_LEVEL=info

# =============================================================================
# AUTOMATION RULES (Optional)
# =============================================================================
# Path to automation rules file (default: priv/rules.json)
RULES_FILE=priv/rules.json
