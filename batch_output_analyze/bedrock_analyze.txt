Based on the code in `lib/claude_agent_sdk/auth/providers/bedrock.ex`:\n\n## What this module does\nHandles AWS Bedrock authentication as an alternative to Anthropic API keys. It verifies AWS credentials are available and returns authentication configuration for the SDK to use Bedrock's Claude models via AWS infrastructure.\n\n## Key functions\n\n1. **`setup_token/0`** (`lib/claude_agent_sdk/auth/providers/bedrock.ex:14`) - Main entry point that validates AWS credentials and returns `{:ok, \"aws-bedrock\", nil}` if configured, otherwise provides setup instructions\n\n2. **`aws_credentials_available?/0`** (truncated in snippet) - Checks for AWS credentials via environment variables or AWS CLI configuration\n\n## Improvement suggestion\n\nAdd validation for the AWS region to ensure it supports Bedrock. Some AWS regions don't have Bedrock available, so checking against a list of supported regions (e.g., `us-east-1`, `us-west-2`, `eu-west-1`) would provide better error messages before attempting to authenticate.
Based on the code in `lib/claude_agent_sdk/auth/providers/bedrock.ex`:\n\n## What this module does\nHandles AWS Bedrock authentication as an alternative to Anthropic API keys. It verifies AWS credentials are available and returns authentication configuration for the SDK to use Bedrock's Claude models via AWS infrastructure.\n\n## Key functions\n\n1. **`setup_token/0`** (`lib/claude_agent_sdk/auth/providers/bedrock.ex:14`) - Main entry point that validates AWS credentials and returns `{:ok, \"aws-bedrock\", nil}` if configured, otherwise provides setup instructions\n\n2. **`aws_credentials_available?/0`** (truncated in snippet) - Checks for AWS credentials via environment variables or AWS CLI configuration\n\n## Improvement suggestion\n\nAdd validation for the AWS region to ensure it supports Bedrock. Some AWS regions don't have Bedrock available, so checking against a list of supported regions (e.g., `us-east-1`, `us-west-2`, `eu-west-1`) would provide better error messages before attempting to authenticate.